// jam로고 컴포넌트
import React from "react";

const Logo = () => {
    const isrc = {
        logo: "./images.logo.svg/"
    }

    return(
        
    );

}; ////////// Logo /////////////

export default Logo;